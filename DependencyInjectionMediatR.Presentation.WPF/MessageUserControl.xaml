<UserControl x:Class="DependencyInjectionMediatR.Presentation.WPF.MessageUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DependencyInjectionMediatR.Presentation.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>            
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="textMesssage"
                 Grid.Column="0"
                 VerticalAlignment="Center"
                 Margin="5"/>
        <Button x:Name="buttonSend"
                Grid.Column="1"
                Content="Send"
                Margin="5"
                Padding="5,3"/>
    </Grid>
</UserControl>
